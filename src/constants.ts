import { TookeyConfig } from "./types";

export const DEFAULT_CONFIG : TookeyConfig = {
    relayUrl: "https://relay1.apps-production.tookey.cloud",
    backendUrl: "https://backend.apps-production.tookey.cloud/",
    keys: []
}